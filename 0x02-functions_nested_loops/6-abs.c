#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer
 * @n: the integer
 * Return: always n (Success)
 */

int _abs(int)
{
	if (n > 0)
		return (n);
	else 
		return (-n);
}
